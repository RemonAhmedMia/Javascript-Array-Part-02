// reverse() হলো JavaScript-এর array method, যেটা কোন array-র উপাদানগুলোর ক্রম উল্টে দেয় (last থেকে first পর্যন্ত)।
// এটা মূল array-টাই পরিবর্তন করে (in-place change)।

let number = [1, 2, 3, 4, 5];
console.log(number);

number.reverse();

console.log(number);  // Output: [5, 4, 3, 2, 1]
